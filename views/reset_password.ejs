<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      href="/public/css/reset_password.css"
      rel="stylesheet"
      type="text/css"
    />
    <title>Reset Password</title>
  </head>
  <body>
    <%- include layout/navbar %>
    <div class="reset">
      <img
        class="other_banner"
        src="/public/images/others_banner.jpg"
        alt="banner_img"
      />
      <div>
        <div class="reset_info">
          <h2>Reset Password</h2>
          <form
            class="reset_form"
            method="POST"
            action="/users/reset_password/:id"
          >
            <% if(messages && messages.length > 0) { %> <%
            messages.forEach(message => { %>
            <span><%= message %></span>
            <% })} %>
            <br />
            <% if (user) { %> <input type="hidden" name="email"
            value=<%=user.email %> /> <% } %>

            <label for="password"> Password: </label>
            <input type="password" name="password" />

            <label for="passwordConfirm"> Confirm Password: </label>
            <input type="password" name="passwordConfirm" />

            <button class="reset_button" type="submit" value="Submit">
              Submit
            </button>
          </form>
        </div>
      </div>
    </div>
    <%- include layout/footer %>
  </body>
</html>
